# Redirect HTTP to HTTPS (will be uncommented after SSL)
# server {
#     listen 80;
#     server_name pendekinlink.fahridanaa.my.id;
#     return 301 https://$server_name$request_uri;
# }

# PendekinLink - HTTP (temporary, before SSL)
server {
    listen 80;
    server_name pendekinlink.fahridanaa.my.id;

    # Certbot challenge
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # Frontend
    location / {
        proxy_pass http://web:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # API
    location /api {
        proxy_pass http://api:4000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Redirect endpoint (short codes)
    location ~ ^/[a-zA-Z0-9]\{6\}$ {
        proxy_pass http://api:4000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# PendekinLink - HTTPS (will be enabled after SSL)
# server {
#     listen 443 ssl http2;
#     server_name pendekinlink.fahridanaa.my.id;
#
#     ssl_certificate /etc/letsencrypt/live/pendekinlink.fahridanaa.my.id/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/pendekinlink.fahridanaa.my.id/privkey.pem;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#     ssl_prefer_server_ciphers on;
#
#     location / {
#         proxy_pass http://web:3000;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
#
#     location /api {
#         proxy_pass http://api:4000;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
#
#     location ~ ^/[a-zA-Z0-9]{6}$ {
#         proxy_pass http://api:4000;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }
